function splitMessage(message, delimeter) {
  let words;
  // Change code below this line
words = message.splite(delimeter);
  // Change code above this line
  return words;
}

console.log(splitMessage('12345', ''));